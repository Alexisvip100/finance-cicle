# Copia a .env y ajusta si vas a correr `docker compose up` con overrides.
# DATABASE_URL y demás ya tienen defaults funcionales en docker-compose.yml;
# esto es solo para valores que sí quieras cambiar (ej. SECRET_KEY en prod).
SECRET_KEY=change-me-in-production
