fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib==1.7.4
# bcrypt>=4.1 quitó `__about__` y bcrypt>=4.1 también volvió error (antes solo
# truncaba) pasar >72 bytes — ambos rompen el self-check interno de passlib
# 1.7.4 (sin mantenimiento desde 2020). 4.0.1 es la última versión compatible.
bcrypt==4.0.1
python-dotenv==1.0.1
alembic==1.14.0
pytest==8.3.4
httpx==0.27.2
# Necesario en prod (Render, Vercel) en cuanto DATABASE_URL apunta a Postgres real.
# En dev/test se queda sin usar (SQLite no lo necesita), pero así Vercel — que
# solo instala este archivo, no requirements-postgres.txt — ya lo trae.
psycopg2-binary==2.9.10
